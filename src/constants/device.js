const DEVICE_TYPE = {
  LARGE_DESKTOP: 'largeDesktop',
  DESKTOP: 'desktop',
  TABLET: 'tablet',
  MOBILE: 'mobile',
};

const LARGE_DESKTOP_SIZE = {
  MIN: 3000,
  MAX: 4000,
  ITEMS: 8,
};

const DESKTOP_SIZE = {
  MIN: 1024,
  MAX: 3000,
  ITEMS: 5,
};

const TABLE_SIZE = {
  MIN: 464,
  MAX: 1024,
  ITEMS: 3,
};

const MOBILE_SIZE = {
  MIN: 0,
  MAX: 464,
  ITEMS: 2,
};

const AUTO_PLAY_SPEED = 2000;

export {
  DEVICE_TYPE,
  LARGE_DESKTOP_SIZE,
  DESKTOP_SIZE,
  TABLE_SIZE,
  MOBILE_SIZE,
  AUTO_PLAY_SPEED,
};
